
<div class="container">
  <% if user_signed_in? %>
  <li>
    <%= link_to 'Sign out', destroy_user_session_path,
                method: :delete, style: "color: red;",
                data: { 'turbo-method': :delete, turbo_confirm: 'Are you sure you want to logout' } %>
  </li>
  <% else %>
  <%= link_to 'Sign in', new_user_session_path, class: 'nav-link', style: "color: green;" %>
  <% end %>
</div>
