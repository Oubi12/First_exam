<h1>Title: <%= @post.title %></h1>
<h2>Edit Comment ID: <%= @comment.id %></h2>
<%= render partial: 'form', locals: { comment: @comment, path: post_comment_path(@post, @comment), method: :patch } %>
<%= link_to 'Back', post_comments_path, class: 'btn btn-outline-dark' %>